model 
{
  for (i in 1:N) 
  {
  z[i] ~ dnorm(y[i], tau2)
  }
  
  tau2 <- pow(sigma2, -2)
  sigma2 ~ dunif(0, 100)

  for (i in connectFore) 
  {
    y[i] ~ dnorm(0, tau)
  }

  for (i in connectBoth) 
  {
    y[i] ~ dnorm( y[i-1] + x[i-1,]%*%beta , tau)T(0,1000000)
  }
  
  for (i in connectBack) 
  {
    y[i] ~ dnorm( y[i-1] + x[i-1,]%*%beta, tau)T(0,1000000)
  }

  
  for (i in 1:p) 
  {
    beta[i] ~ dnorm(0, .0001)
  }
  
  tau <- pow(sigma, -2)
  sigma ~ dunif(0, 100)
  }